<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="userMapper">

    <!-- Beans의 멤버변수(property)이름과 대상 테이블의 컬럼(column)을 연결한다. -->
    <resultMap id="userMapper"
        type="study.spring.hellobook.model.User">
        <result property="id"   column="id" />
        <result property="pw"     column="pw" />
        <result property="name"   column="name" />
        <result property="email" column="email" />
    </resultMap>

    <!-- 데이터 수 조회를 위한 기능 정의 -->
    <select id="selectCountAll"
        parameterType="study.spring.hellobook.model.User" resultType="int">
        SELECT COUNT (*)
        FROM user
    </select>
    
     <!-- 단일행 조회를 위한 기능 정의 -->
    <select id="selectItem" parameterType="study.spring.hellobook.model.User"
        resultMap="userMapper">
        SELECT * FROM user WHERE id=#{id};
    </select>
    
    <!--  로그인 아이디 패스워드 확인 -->
	<select id="idPwCheck" parameterType = "study.spring.hellobook.model.User" resultMap="userMapper">
		SELECT id, email, name, pw FROM user WHERE email = #{email} AND pw = #{pw}
	</select>
	
	<select id="pwCheck" parameterType = "study.spring.hellobook.model.User" resultMap="userMapper">
		SELECT email, pw FROM user WHERE email = #{email}
	</select>
	
	<!-- 회원정보 저장을 위한 기능 정의 -->
	<insert id="insertUser"
		parameterType = "study.spring.hellobook.model.User"
		useGeneratedKeys="true" keyProperty="id">
		INSERT INTO user(email, pw, name)
		VALUES(#{email}, #{pw}, #{name}
		)
	</insert>
	
	<!--  아이디 중복 확인을 위한 기능 정의 -->
	<select id="idCheck" parameterType = "study.spring.hellobook.model.User" resultType="int">
		SELECT COUNT(*) FROM user WHERE email = #{email}
	</select>

</mapper>
